using UnityEngine;
using System.Collections;

public class lightPosition : MonoBehaviour {

   /* // Use this for initialization
    GameObject objectPosition;
    Vector3 position;
	void Start () {
        objectPosition = GameObject.FindGameObjectWithTag("CastShadow");
	}
	
	// Update is called once per frame
	void Update () {
        transform.position = objectPosition.GetComponent<raycastTest>().center;
	
	}*/
}
